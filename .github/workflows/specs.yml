name: Specs
on: [push]
jobs:
   test:
     name: Fetch
     runs-on: ubuntu-latest
     container: ubuntu:20.10
     env:
       DEBIAN_FRONTEND: noninteractive
     strategy:
       fail-fast: false
       matrix:
         runner: [0]
     steps:
       - run: |
           apt-get install -y lshw
           lshw -C display
